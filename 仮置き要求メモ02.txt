なかなか良くなってきている。
さらに下記の内容を反映して修正および追加すること。
HTMLコードは一括貼り付けによる操作で完結したいため、すべてを反映した全文のコードにする。一部だけは禁止。
そして指示のない他の変更は一切禁止とする。

<1>店舗登録のポップアップ入力の形式を以下の内容に変更する。
・リスト登録は既存のリストがあれば追加用プルダウンに表示され選択できる。反映自体は下部の登録保存をクリックまたはタッチしたときにリストへ反映されるようにする。ポップアウト入力の自体はまだ反映先のリストを指定するだけにしておく。
⇒既存リストが複数ある場合は横の＋マークを押すとさらに登録用の行が追加される。

・新規リストを作成する場合、既存リストとは別にある1つ下の名称入力用の欄に記載する。反映自体は下部の登録保存をクリックまたはタッチしたときにリストへ反映されるようにする。ポップアウト入力の自体はまだ反映先のリストを指定するだけにしておく。
⇒既存リストが複数ある場合は横の＋マークを押すとさらに登録用の行が追加される。

・コスパ、キャパ、雰囲気のバーがずれていて景観が良くないため、位置とサイズを綺麗に揃え統一する。